import React from 'react';  //Import React every time we go to use HTML insede of JS
import ReactDOM from 'react-dom'; //Communication with elements of HTML
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (JavaScript + HTML, XML)

// Renderizar = ingetando, desenhando
// Desestruturar = pegar um objeto e dividi-lo em variaveis 